# Modeling Design

OOF-first hybrid modeling:

- Tabular model: handcrafted/retrieval/semantic numeric features.
- Cross-encoder model: pair text formatting with semantic relevance.
- Ensemble: OOF weighted average.

Scripts: `train_tabular.py`, `train_cross_encoder.py`, `train_ensemble.py`.
