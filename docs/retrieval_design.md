# Retrieval Design

Modüller: BM25, dense fallback, hybrid retriever.

Kullanım: hard negative pools, pseudo-label candidates, retrieval features, error analysis.

Index script: `scripts_kaggle/build_retrieval_index.py`.
