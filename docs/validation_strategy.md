# Validation Strategy

Private-LB oriented validation:

- `term_group` / `query_group` split
- OOF predictions
- global threshold tuning
- segment scores
- seed stability

Script: `scripts_kaggle/run_validation.py` or `scripts_kaggle/run_experiment.py`.
