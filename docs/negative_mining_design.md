# Negative Mining Design

Modüller: `negative_pools.py`, `negative_filters.py`, `negative_mining.py`.

Türler: easy, same-category, same-brand, lexical-confusing, attribute-conflict, dense-hard hook.

False-negative safety: known positive exclusion, high lexical/dense uncertainty, same brand/category variant risk, conflict flags.
