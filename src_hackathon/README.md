# Hackathon / Final Mode

This namespace is reserved for final-stage work: 3-class logic, serving/API,
ONNX/quantization, explainability UI, demos, latency optimization and deployment.

Current legacy implementation remains under `src/` to avoid breaking old code.
Kaggle War Mode must not import from `src_hackathon/` or legacy `src/`.
